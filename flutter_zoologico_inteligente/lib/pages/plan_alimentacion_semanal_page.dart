
import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';

class PlanAlimentacionPage extends StatelessWidget {
  const PlanAlimentacionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Plan de Alimentación Semanal')),
      body: const Center(child: Text('')),
    );
  }
}
